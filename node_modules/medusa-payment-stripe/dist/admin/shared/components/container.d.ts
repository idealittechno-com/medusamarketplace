import { PropsWithChildren, ReactNode } from "react";
type Props = PropsWithChildren<{
    title: string;
    description?: string;
    icon?: ReactNode;
}>;
export declare const Container: ({ title, description, icon, children }: Props) => import("react/jsx-runtime").JSX.Element;
export {};
